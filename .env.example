# Option 1 (recommandé): base host only, and paths include /monatis/...
VITE_API_BASE_URL=http://localhost:8082

# Option 2: if your backend is served under /monatis (rare),
# set base to .../monatis and paths will be de-duplicated automatically.
# VITE_API_BASE_URL=http://localhost:8082/monatis
